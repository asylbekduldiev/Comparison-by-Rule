package com.example.diploma.service;import org.apache.poi.xwpf.usermodel.*;import org.apache.poi.xwpf.usermodel.XWPFTable;import org.apache.poi.xwpf.usermodel.XWPFTableRow;import org.apache.poi.xwpf.usermodel.XWPFTableCell;import org.springframework.stereotype.Service;import java.io.ByteArrayInputStream;import java.util.*;import java.util.regex.Matcher;import java.util.regex.Pattern;@Servicepublic class FileService {  // Структурные элементы диссертации в правильном порядке  private static final List<String> REQUIRED_SECTIONS = Arrays.asList(    "содержание", "нормативные ссылки", "определения",    "обозначения и сокращения", "введение", "заключение",    "список использованных источников"  );  private static final int MIN_SOURCES = 25;  public String analyzeWordFile(byte[] content) throws Exception {    StringBuilder report = new StringBuilder("ОТЧЕТ ПО АНАЛИЗУ МАГИСТЕРСКОЙ ДИССЕРТАЦИИ\n");    report.append("Согласно требованиям П МУА 021-23\n");    report.append("================================================\n\n");    boolean hasErrors = false;    Map<String, Object> analysisResults = new HashMap<>();    try (XWPFDocument doc = new XWPFDocument(new ByteArrayInputStream(content))) {      // 1. Проверка структуры документа      hasErrors |= checkDocumentStructure(doc, report, analysisResults);      // 2. Проверка титульного листа      hasErrors |= checkTitlePage(doc, report);      // 3. Проверка оформления текста      hasErrors |= checkTextFormatting(doc, report);      // 4. Проверка объема диссертации      hasErrors |= checkDocumentVolume(doc, report, analysisResults);      // 5. Проверка введения      hasErrors |= checkIntroduction(doc, report);      // 6. Проверка основной части      hasErrors |= checkMainPart(doc, report);      // 7. Проверка заключения      hasErrors |= checkConclusion(doc, report);      // 8. Проверка списка источников      hasErrors |= checkSources(doc, report);      // 9. Проверка ссылок в тексте      hasErrors |= checkCitations(doc, report);      // 10. Проверка нумерации страниц      hasErrors |= checkPageNumbering(doc, report);      // 11. Проверка таблиц и рисунков      hasErrors |= checkTablesAndFigures(doc, report);      // 12. Проверка формул      hasErrors |= checkFormulas(doc, report);      // Итоговый отчет      report.append("\n================================================\n");      report.append("ИТОГОВАЯ СВОДКА:\n");      report.append("================================================\n");      if (!hasErrors) {        report.append("✅ ДИССЕРТАЦИЯ СООТВЕТСТВУЕТ ВСЕМ ОСНОВНЫМ ТРЕБОВАНИЯМ\n");        report.append("Документ готов к предварительной защите.\n");      } else {        report.append("❌ ОБНАРУЖЕНЫ НАРУШЕНИЯ ТРЕБОВАНИЙ\n");        report.append("Необходимо устранить указанные ошибки перед защитой.\n");      }      // Статистика      if (analysisResults.containsKey("totalPages")) {        report.append("\nСТАТИСТИКА ДОКУМЕНТА:\n");        report.append("- Общий объем: ").append(analysisResults.get("totalPages")).append(" страниц\n");      }      if (analysisResults.containsKey("sourcesCount")) {        report.append("- Количество источников: ").append(analysisResults.get("sourcesCount")).append("\n");      }    }    return report.toString();  }  /**   * Проверка структуры документа на наличие обязательных разделов   */  private boolean checkDocumentStructure(XWPFDocument doc, StringBuilder report, Map<String, Object> results) {    report.append("1. ПРОВЕРКА СТРУКТУРЫ ДОКУМЕНТА:\n");    report.append("----------------------------------------\n");    boolean hasErrors = false;    Set<String> foundSections = new HashSet<>();    List<XWPFParagraph> paragraphs = doc.getParagraphs();    for (XWPFParagraph para : paragraphs) {      String text = para.getText().toLowerCase().trim();      for (String section : REQUIRED_SECTIONS) {        if (text.contains(section) && !foundSections.contains(section)) {          foundSections.add(section);          report.append("  ✅ Найден раздел: \"").append(section.toUpperCase()).append("\"\n");        }      }    }    for (String requiredSection : REQUIRED_SECTIONS) {      if (!foundSections.contains(requiredSection)) {        report.append("  ❌ ОТСУТСТВУЕТ ОБЯЗАТЕЛЬНЫЙ РАЗДЕЛ: \"").append(requiredSection.toUpperCase()).append("\"\n");        hasErrors = true;      }    }    report.append("\n");    return hasErrors;  }  /**   * Проверка титульного листа   */  private boolean checkTitlePage(XWPFDocument doc, StringBuilder report) {    report.append("2. ПРОВЕРКА ТИТУЛЬНОГО ЛИСТА:\n");    report.append("----------------------------------------\n");    boolean hasErrors = false;    List<XWPFParagraph> firstPageParagraphs = new ArrayList<>();    // Собираем первые 10 параграфов (предполагаем, что это титульный лист)    List<XWPFParagraph> paragraphs = doc.getParagraphs();    for (int i = 0; i < Math.min(10, paragraphs.size()); i++) {      firstPageParagraphs.add(paragraphs.get(i));    }    String fullTitleText = firstPageParagraphs.stream()      .map(XWPFParagraph::getText)      .reduce("", (a, b) -> a + " " + b)      .toLowerCase();    // Обязательные элементы титульного листа    Map<String, String> requiredElements = new HashMap<>();    requiredElements.put("университет", "международный университет астана");    requiredElements.put("степень", "магистр");    requiredElements.put("научный руководитель", "научный руководитель");    requiredElements.put("город", "астана");    for (Map.Entry<String, String> element : requiredElements.entrySet()) {      if (fullTitleText.contains(element.getValue())) {        report.append("  ✅ ").append(element.getKey().toUpperCase()).append(" указан корректно\n");      } else {        report.append("  ❌ ОТСУТСТВУЕТ: ").append(element.getKey().toUpperCase()).append("\n");        hasErrors = true;      }    }    report.append("\n");    return hasErrors;  }  /**   * Проверка форматирования текста согласно требованиям   */  private boolean checkTextFormatting(XWPFDocument doc, StringBuilder report) {    report.append("3. ПРОВЕРКА ФОРМАТИРОВАНИЯ ТЕКСТА:\n");    report.append("----------------------------------------\n");    boolean hasErrors = false;    List<XWPFParagraph> paragraphs = doc.getParagraphs();    int checkedParagraphs = 0;    for (int i = 0; i < Math.min(50, paragraphs.size()); i++) { // Проверяем первые 50 параграфов      XWPFParagraph para = paragraphs.get(i);      String text = para.getText().trim();      if (text.isEmpty() || text.length() < 10) continue;      checkedParagraphs++;      // Проверка размера шрифта (если доступно)      List<XWPFRun> runs = para.getRuns();      for (XWPFRun run : runs) {        if (run.getFontSize() != -1 && run.getFontSize() != 14) {          report.append("  ⚠️ Размер шрифта ").append(run.getFontSize()).append(" (требуется 14)\n");          hasErrors = true;          break;        }      }      // Проверка двойных пробелов      if (text.contains("  ")) {        report.append("  ❌ Двойные пробелы в параграфе: \"").append(text.substring(0, Math.min(50, text.length()))).append("...\"\n");        hasErrors = true;      }      // Проверка пробелов перед знаками препинания      Pattern p = Pattern.compile(" [,.;:!?]");      Matcher m = p.matcher(text);      if (m.find()) {        report.append("  ❌ Пробел перед знаком препинания: \"").append(m.group()).append("\"\n");        hasErrors = true;      }      if (checkedParagraphs >= 20) break; // Ограничиваем для оптимизации    }    if (!hasErrors) {      report.append("  ✅ Форматирование текста соответствует требованиям\n");    }    report.append("\n");    return hasErrors;  }  /**   * Проверка объема диссертации   */  private boolean checkDocumentVolume(XWPFDocument doc, StringBuilder report, Map<String, Object> results) {    report.append("4. ПРОВЕРКА ОБЪЕМА ДИССЕРТАЦИИ:\n");    report.append("----------------------------------------\n");    // Подсчет страниц (приблизительно)    int totalParagraphs = doc.getParagraphs().size();    int estimatedPages = Math.max(1, totalParagraphs / 25); // Примерно 25 параграфов на страницу    results.put("totalPages", estimatedPages);    boolean hasErrors = false;    report.append("  📄 Приблизительный объем: ").append(estimatedPages).append(" страниц\n");    // Для гуманитарных направлений: 70-90 страниц    // Для естественных и технических: 60-70 страниц    if (totalParagraphs < 60) {      report.append("  ❌ ОБЪЕМ НЕДОСТАТОЧЕН (минимум 60 страниц)\n");      hasErrors = true;    } else if (estimatedPages > 90) {      report.append("  ⚠️ Объем превышает рекомендуемый максимум (90 страниц)\n");    } else {      report.append("  ✅ Объем соответствует требованиям\n");    }    report.append("\n");    return hasErrors;  }  /**   * Проверка введения   */  private boolean checkIntroduction(XWPFDocument doc, StringBuilder report) {    report.append("5. ПРОВЕРКА ВВЕДЕНИЯ:\n");    report.append("----------------------------------------\n");    boolean hasErrors = false;    String introText = findSectionText(doc, "введение");    if (introText == null || introText.trim().isEmpty()) {      report.append("  ❌ ВВЕДЕНИЕ НЕ НАЙДЕНО\n");      return true;    }    // Обязательные элементы введения согласно п. 4.3.6    String[] requiredElements = {      "актуальность", "цель", "задач", "объект", "предмет",      "метод", "значимость", "практическая значимость"    };    String lowerIntro = introText.toLowerCase();    int foundElements = 0;    for (String element : requiredElements) {      if (lowerIntro.contains(element)) {        foundElements++;        report.append("  ✅ ").append(element.toUpperCase()).append(" - присутствует\n");      } else {        report.append("  ❌ ").append(element.toUpperCase()).append(" - отсутствует\n");        hasErrors = true;      }    }    // Проверка объема введения (должно быть 1-2 страницы)    int introLength = introText.length();    if (introLength < 1000) {      report.append("  ⚠️ Введение может быть слишком кратким (").append(introLength).append(" символов)\n");    } else if (introLength > 5000) {      report.append("  ⚠️ Введение может быть слишком объемным (").append(introLength).append(" символов)\n");    }    report.append("  📊 Найдено ").append(foundElements).append(" из ").append(requiredElements.length).append(" обязательных элементов\n");    report.append("\n");    return hasErrors;  }  /**   * Проверка основной части   */  private boolean checkMainPart(XWPFDocument doc, StringBuilder report) {    report.append("6. ПРОВЕРКА ОСНОВНОЙ ЧАСТИ:\n");    report.append("----------------------------------------\n");    boolean hasErrors = false;    // Поиск разделов с номерами (1, 2, 3 и т.д.)    List<XWPFParagraph> paragraphs = doc.getParagraphs();    int sectionsFound = 0;    Pattern sectionPattern = Pattern.compile("^\\s*[1-9]\\s+[А-Я].*", Pattern.UNICODE_CHARACTER_CLASS);    for (XWPFParagraph para : paragraphs) {      String text = para.getText().trim();      if (sectionPattern.matcher(text).matches()) {        sectionsFound++;        report.append("  ✅ Найден раздел: \"").append(text).append("\"\n");      }    }    if (sectionsFound < 2) {      report.append("  ❌ НЕДОСТАТОЧНО РАЗДЕЛОВ (найдено ").append(sectionsFound).append(", требуется минимум 2)\n");      hasErrors = true;    } else {      report.append("  ✅ Количество разделов соответствует требованиям (").append(sectionsFound).append(")\n");    }    report.append("\n");    return hasErrors;  }  /**   * Проверка заключения   */  private boolean checkConclusion(XWPFDocument doc, StringBuilder report) {    report.append("7. ПРОВЕРКА ЗАКЛЮЧЕНИЯ:\n");    report.append("----------------------------------------\n");    String conclusionText = findSectionText(doc, "заключение");    if (conclusionText == null || conclusionText.trim().isEmpty()) {      report.append("  ❌ ЗАКЛЮЧЕНИЕ НЕ НАЙДЕНО\n");      return true;    }    boolean hasErrors = false;    String lowerConclusion = conclusionText.toLowerCase();    // Обязательные элементы заключения согласно п. 4.3.8    String[] requiredElements = {      "выводы", "результат", "рекомендаци", "эффективность"    };    int foundElements = 0;    for (String element : requiredElements) {      if (lowerConclusion.contains(element)) {        foundElements++;        report.append("  ✅ ").append(element.toUpperCase()).append(" - присутствует\n");      }    }    if (foundElements < 2) {      report.append("  ⚠️ В заключении найдено мало ключевых элементов (").append(foundElements).append(" из ").append(requiredElements.length).append(")\n");    }    report.append("  📊 Объем заключения: ").append(conclusionText.length()).append(" символов\n");    report.append("\n");    return hasErrors;  }  /**   * Проверка списка источников   */  private boolean checkSources(XWPFDocument doc, StringBuilder report) {    report.append("8. ПРОВЕРКА СПИСКА ИСТОЧНИКОВ:\n");    report.append("----------------------------------------\n");    boolean hasErrors = false;    String sourcesText = findSectionText(doc, "список использованных источников");    if (sourcesText == null || sourcesText.trim().isEmpty()) {      report.append("  ❌ СПИСОК ИСТОЧНИКОВ НЕ НАЙДЕН\n");      return true;    }    // Подсчет количества источников    Pattern sourcePattern = Pattern.compile("^\\s*\\d+\\s+", Pattern.MULTILINE);    Matcher matcher = sourcePattern.matcher(sourcesText);    int sourcesCount = 0;    while (matcher.find()) {      sourcesCount++;    }    report.append("  📚 Количество источников: ").append(sourcesCount).append("\n");    if (sourcesCount < MIN_SOURCES) {      report.append("  ❌ НЕДОСТАТОЧНО ИСТОЧНИКОВ (минимум ").append(MIN_SOURCES).append(")\n");      hasErrors = true;    } else {      report.append("  ✅ Количество источников соответствует требованиям\n");    }    report.append("\n");    return hasErrors;  }  /**   * Проверка ссылок в тексте   */  private boolean checkCitations(XWPFDocument doc, StringBuilder report) {    report.append("9. ПРОВЕРКА ССЫЛОК В ТЕКСТЕ:\n");    report.append("----------------------------------------\n");    boolean hasErrors = false;    List<XWPFParagraph> paragraphs = doc.getParagraphs();    int citationsFound = 0;    // Поиск ссылок в квадратных скобках [1], [2, 3] и т.д.    Pattern citationPattern = Pattern.compile("\\[\\d+(,\\s*\\d+)*\\]");    for (XWPFParagraph para : paragraphs) {      String text = para.getText();      Matcher matcher = citationPattern.matcher(text);      while (matcher.find()) {        citationsFound++;      }    }    report.append("  🔗 Найдено ссылок: ").append(citationsFound).append("\n");    if (citationsFound < 10) {      report.append("  ⚠️ Мало ссылок на источники в тексте\n");    } else {      report.append("  ✅ Использование ссылок в тексте соответствует требованиям\n");    }    report.append("\n");    return hasErrors;  }  /**   * Проверка нумерации страниц   */  private boolean checkPageNumbering(XWPFDocument doc, StringBuilder report) {    report.append("10. ПРОВЕРКА НУМЕРАЦИИ СТРАНИЦ:\n");    report.append("----------------------------------------\n");    // Проверка наличия номеров страниц в колонтитулах    List<XWPFFooter> footers = doc.getFooterList();    boolean hasPageNumbers = false;    for (XWPFFooter footer : footers) {      String footerText = footer.getText().toLowerCase();      if (footerText.matches(".*\\d+.*")) {        hasPageNumbers = true;        break;      }    }    if (hasPageNumbers) {      report.append("  ✅ Нумерация страниц присутствует\n");    } else {      report.append("  ⚠️ Нумерация страниц не обнаружена в колонтитулах\n");    }    report.append("\n");    return false; // Не критическая ошибка  }  /**   * Проверка таблиц и рисунков   */  private boolean checkTablesAndFigures(XWPFDocument doc, StringBuilder report) {    report.append("11. ПРОВЕРКА ТАБЛИЦ И РИСУНКОВ:\n");    report.append("----------------------------------------\n");    boolean hasErrors = false;    // Проверка таблиц    List<XWPFTable> tables = doc.getTables();    report.append("  📊 Количество таблиц: ").append(tables.size()).append("\n");    // Проверка подписей к рисункам    List<XWPFParagraph> paragraphs = doc.getParagraphs();    int figuresCount = 0;    Pattern figurePattern = Pattern.compile("рисунок\\s+\\d+", Pattern.CASE_INSENSITIVE | Pattern.UNICODE_CHARACTER_CLASS);    for (XWPFParagraph para : paragraphs) {      String text = para.getText().toLowerCase();      if (figurePattern.matcher(text).find()) {        figuresCount++;      }    }    report.append("  🖼️ Количество рисунков: ").append(figuresCount).append("\n");    if (tables.size() > 0 || figuresCount > 0) {      report.append("  ✅ Иллюстративный материал присутствует\n");    } else {      report.append("  ⚠️ Отсутствуют таблицы и рисунки\n");    }    report.append("\n");    return hasErrors;  }  /**   * Проверка формул   */  private boolean checkFormulas(XWPFDocument doc, StringBuilder report) {    report.append("12. ПРОВЕРКА ФОРМУЛ:\n");    report.append("----------------------------------------\n");    List<XWPFParagraph> paragraphs = doc.getParagraphs();    int formulasCount = 0;    // Поиск формул (упрощенно - по наличию математических символов)    Pattern formulaPattern = Pattern.compile("[=+\\-*/^(){}\\[\\]∑∫∏√].*[=+\\-*/^(){}\\[\\]∑∫∏√]");    for (XWPFParagraph para : paragraphs) {      String text = para.getText();      if (formulaPattern.matcher(text).find() && text.trim().length() > 5) {        formulasCount++;      }    }    report.append("  🔢 Найдено формул: ").append(formulasCount).append("\n");    if (formulasCount > 0) {      report.append("  ✅ Математический аппарат использован\n");    } else {      report.append("  ℹ️ Формулы не обнаружены\n");    }    report.append("\n");    return false; // Не критическая ошибка  }  /**   * Поиск текста определенного раздела   */  private String findSectionText(XWPFDocument doc, String sectionName) {    List<XWPFParagraph> paragraphs = doc.getParagraphs();    StringBuilder sectionText = new StringBuilder();    boolean inSection = false;    for (XWPFParagraph para : paragraphs) {      String text = para.getText().trim();      String lowerText = text.toLowerCase();      if (lowerText.equals(sectionName) || lowerText.startsWith(sectionName)) {        inSection = true;        continue;      }      if (inSection) {        // Прекращаем сбор текста при встрече следующего раздела        if (text.matches("^[А-Я][А-Я\\s]+$") && text.length() < 100) {          break;        }        sectionText.append(text).append(" ");      }    }    return sectionText.toString().trim();  }}